/* Finding minimum between two numbers */
a, b: float;
c, d: int;

{
    input(a);
    input(b);

    while (a > 5)
        if (b > 6)
           output(a);
        else
           output(b);

    c = 3 * 2.0;
    c = 6 - 1;

    d = cast<int>(b);

    if (!(a < b) || a < b)
        output(a);
    else
        output(b);
}
